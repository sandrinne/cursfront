// 6. Fes un programa que demani a l’usuari l’altura i el pes i mostri per pantalla el seu índex de massa corporal (IMC)
let a=+prompt ("Escribe tu altura en m");
let b=+prompt ("escribe tu peso en Kgrs");
let imc=b/a;
document.write(imc);
// la mia : let imc = peso / (altura ^ 2)
// ((altura/100)**2);
