// 5. Fes un programa que demani un número a l’usuari i calculi si és parell o senar.
let a=prompt("Introduce un número");
let total=a%2;
if (total == 0) {
    window.alert (a + " es par");
} else {
    window.alert(a + "es impar");
};
